name: flutter_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  equations: ^5.0.1
  flutter_math_fork: ^0.7.2
  provider: ^6.0.5
  sprintf: "^7.0.0"
  flutter_switch: ^0.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/smith_original_picture.jpg